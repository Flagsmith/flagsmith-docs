{
    "openapi": "3.0.3",
    "info": { "title": "SDK endpoints", "version": "1" },

    "paths": {
        "/flags": {
            "get": {
                "tags": [],
                "summary": "Get flags",
                "description": "A `GET` request sent to the flags endpoint returns a list of feature flags for the authenticated environment.",
                "responses": { "200": { "description": "ok" } }
            }
        },
        "/identities": {
            "get": {
                "summary": "Get identity flags"
            },
            "post": {
                "summary": "Get identities flags (with traits)"
            }
        }
    },
    "components": {
        "securitySchemes": {
            "EnvironmentKeyAuth": { "type": "apiKey", "in": "header", "name": "X-Environment-Key" }
        },
        "schemas": {}
    },
    "security": [{ "EnvironmentKeyAuth": [] }],
    "tags": []
}
